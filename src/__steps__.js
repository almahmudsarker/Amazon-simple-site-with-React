/**
 * 1. create firebase project
 * 2. create firebase web app
 * 3. npm i firbase
 * 4. save firebase config and export default app
 * 5. firebase web> build> authentication> enable signIn/google
 * 6. create signup and sihnin route
 */

/**
 * 1. create a context provider file
 * 2. create a context and set provider
 * 3. set the children props
 * 4. set context value
 * 5. set the provider 
 */

/**
 * HOSTING
 * --------
 * one time per pc
 * 1. npm i -g firebase-tools
 * 2. firebase login for one pc 
 * ----------
 * for each project one time
 * 1. firebase init
 * 2. select hosting
 * 3. select existing project
 * 4. set build folder as dist/build
 * 5. single page app: yes
 * 6. continuos deployment: no
 * ------------
 * for each deployment
 * 1. npm run build
 * 2. firebase deploy
 */